import TypeProduct from "@/app/api/products/type/typeProducts";

export default function SelectFormBuy({ options }: { options: TypeProduct[] }) {
  return (
    <select name="" id="">
      <option value="">Seleccionar Producto</option>
    </select>
  );
}
